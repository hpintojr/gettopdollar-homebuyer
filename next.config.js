/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // The 'experimental' block is removed as 'appDir: true' is the default in Next.js 14
};
module.exports = nextConfig;